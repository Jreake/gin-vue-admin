/**
 * 网站配置文件
 */

const config = {
  appName: "Gin-Vue-Admin",
  appLogo: "",
  showViteLogo: true,
};

export const viteLogo = (env) => {
  if (config.showViteLogo) {
  }
};

export default config;
